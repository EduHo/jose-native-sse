# Defaults used when the consuming app's rootProject.ext does not define them.
# Values mirror React Native 0.86.x (Expo SDK 57).
NativeSse_kotlinVersion=2.1.20
NativeSse_compileSdkVersion=36
NativeSse_minSdkVersion=24
