"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const config_plugins_1 = require("@expo/config-plugins");
const fs = require("fs");
const path = require("path");
const NETWORK_SECURITY_CONFIG_RESOURCE = 'network_security_config';
// ─── iOS ──────────────────────────────────────────────────────────────────────
const withIosNativeSse = (config, { allowCleartext = false, cleartextDomains = [] } = {}) => (0, config_plugins_1.withInfoPlist)(config, (c) => {
    if (!allowCleartext && cleartextDomains.length === 0)
        return c;
    const ats = {
        ...(c.modResults.NSAppTransportSecurity ?? {}),
    };
    if (allowCleartext) {
        ats.NSAllowsArbitraryLoads = true;
    }
    if (cleartextDomains.length > 0) {
        const domains = {
            ...(ats.NSExceptionDomains ?? {}),
        };
        for (const domain of cleartextDomains) {
            domains[domain] = {
                NSExceptionAllowsInsecureHTTPLoads: true,
                // Subdomains are not included: an exception should be as narrow as
                // the endpoint that needs it.
                NSIncludesSubdomains: false,
            };
        }
        ats.NSExceptionDomains = domains;
    }
    c.modResults.NSAppTransportSecurity = ats;
    return c;
});
// ─── Android ──────────────────────────────────────────────────────────────────
const withAndroidNativeSse = (config, { allowCleartext = false, cleartextDomains = [] } = {}) => (0, config_plugins_1.withAndroidManifest)(config, (c) => {
    // Any network access needs INTERNET.
    config_plugins_1.AndroidConfig.Permissions.ensurePermissions(c.modResults, [
        'android.permission.INTERNET',
    ]);
    const app = config_plugins_1.AndroidConfig.Manifest.getMainApplication(c.modResults);
    if (!app?.$)
        return c;
    if (allowCleartext) {
        app.$['android:usesCleartextTraffic'] = 'true';
    }
    if (cleartextDomains.length > 0) {
        // A per-domain config is authoritative on API 24+, so the blanket
        // usesCleartextTraffic flag stays off.
        app.$['android:networkSecurityConfig'] =
            `@xml/${NETWORK_SECURITY_CONFIG_RESOURCE}`;
    }
    return c;
});
/**
 * Writes `res/xml/network_security_config.xml` with one `domain-config` per
 * allowed host. Android has no config-plugin helper for this file, so it is
 * written directly.
 */
const withAndroidNetworkSecurityConfig = (config, { cleartextDomains = [] } = {}) => {
    if (cleartextDomains.length === 0)
        return config;
    return (0, config_plugins_1.withDangerousMod)(config, [
        'android',
        async (c) => {
            const xmlDir = path.join(c.modRequest.platformProjectRoot, 'app', 'src', 'main', 'res', 'xml');
            await fs.promises.mkdir(xmlDir, { recursive: true });
            const entries = cleartextDomains
                .map((domain) => `  <domain-config cleartextTrafficPermitted="true">\n` +
                `    <domain includeSubdomains="false">${domain}</domain>\n` +
                `  </domain-config>`)
                .join('\n');
            const xml = `<?xml version="1.0" encoding="utf-8"?>\n` +
                `<!-- Generated by jose-native-sse. Edits will be overwritten by prebuild. -->\n` +
                `<network-security-config>\n` +
                `${entries}\n` +
                `</network-security-config>\n`;
            await fs.promises.writeFile(path.join(xmlDir, `${NETWORK_SECURITY_CONFIG_RESOURCE}.xml`), xml, 'utf8');
            return c;
        },
    ]);
};
// ─── Combined plugin ──────────────────────────────────────────────────────────
const withNativeSse = (config, options) => {
    const opts = options ?? {};
    if (opts.allowCleartext && (opts.cleartextDomains?.length ?? 0) > 0) {
        console.warn('[jose-native-sse] Both allowCleartext and cleartextDomains were set. ' +
            'allowCleartext already permits every host, so the per-domain list adds ' +
            'nothing — drop allowCleartext to keep the exception narrow.');
    }
    config = withIosNativeSse(config, opts);
    config = withAndroidNativeSse(config, opts);
    config = withAndroidNetworkSecurityConfig(config, opts);
    return config;
};
exports.default = (0, config_plugins_1.createRunOncePlugin)(withNativeSse, 'jose-native-sse');
